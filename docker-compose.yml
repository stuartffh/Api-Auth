version: '3.8'

services:
  contaazul-auth:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      # Persiste o banco de dados SQLite
      - ./auth_logs.db:/app/auth_logs.db
    restart: unless-stopped
